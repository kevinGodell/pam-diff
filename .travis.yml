language: node_js
dist: xenial
#osx_image: xcode10.2
matrix:
  include:
    - os: windows
      node_js:
        - 9
before_script:
  - cat build/addon.target.mk == windows fail
  - npm --version
#  - npm i -g npm == windows fail
#  - npm --version
  - node --version
  - npm install ffbinaries
  - ffbinaries
script:
  - node tests/windows.js
#  - npm run draw:big
#  - npm run draw:big:gc
#  - npm test
#  - npm run gray:regions:5
#  - npm run gray:regions:6
cache:
  directories:
    - node_modules
after_success:
#  - npm run examples
#  - npm run jpeg